<div class="block">
  <div class="secondary-navigation">
  </div>          
  <div class="content">          
    <h2 class="title"><%= t("web-app-theme.all", :default => "All")  %> Questions</h2>
    <div class="inner">
      <table class="table">
        <tr>
        	<th>Question</th>
			<th>Created By</th>
            <th>Created at</th>
            <th class="last">&nbsp;</th>
        </tr>
        <% @questions.each do |question| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= "INACTIVE --" unless question.active %><%= question.question %>
          </td>
          <td>
            <%= question.user.login %>
          </td>
          <td>
            <%= question.created_at %>
          </td>
          <td class="last">
            <%= link_to "Inactivate", admin_question_path(question), :method => :delete, :confirm => "The question will remain in the database but will not be shown in the interview. Continue?" %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar wat-cf">
        <div class="actions">
		    <div class="inner">
			      <% form_for :question, :url => admin_questions_path, :html => { :class => :form } do |f| -%>
					  <div class="group">
					    <%= f.label :question, "New Question", :class => :label %>
					    <%= f.text_field :question, :class => 'text_field' %>
						<%= submit_tag "Create" %>
					  </div>
			      <% end -%>
			</div>
        </div>
        
      </div>
    </div>
  </div>
</div>

